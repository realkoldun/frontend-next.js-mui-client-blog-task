import { checkImage } from '@/api/checkImage';
import { emailSubmit } from '@/api/emailSubmit';
import { getAllPosts } from '@/api/getAllPosts';
import { getFeaturePost } from '@/api/getFeaturePost';
import { getPostBuId } from '@/api/getPostBuId';
import { getSimilarPosts } from '@/api/getSimilarPosts';

export {
    getSimilarPosts,
    getAllPosts,
    getPostBuId,
    getFeaturePost,
    checkImage,
    emailSubmit,
};
